
<<[32mgtdbtk[0m>>

    version:		v2.5.2
    avaliable database:	/share/data1/Database/gtdbtk/release226
    conda env:		source activate /share/data1/zhangy2/conda/envs/gtdbtk.v2.5.2
    [31mcommon command:[0m	
            1、设置数据库路径
                \033[33mexport GTDBTK_DATA_PATH=/share/data1/Database/gtdbtk/release226\033[0m
            2、然后再进行注释
                gtdbtk classify_wf --cpus 80 --genome_dir <input_dir> --out_dir <output_dir> -x <suffix_of_input_genome[fa,fna,...]>
    NOTE:
        相对于v2.3.2，这个版本使用了skani进行ANI计算，他的速度很快

    ----------------------------
         << 其他相关命令 >>
    ----------------------------
    ## 1.列表

    gtdbtk classify_wf --cpus 80 --batchfile <input_table> --out_dir <output_dir>

    用户可以输入基因组列表以运行gtdbtk，列表格式如下,使用tab分割：
    /path/to/genome1.fna.gz	genome1
    /path/to/genome2.fna	genome2
    /path/to/genome3.fa.gz	genome3
    /path/to/genome4.fa	genome4


    ## 2.环境创建
    原始的v2.5.2的软件中，python版本不适配，如果要迁移环境，应当使用一下命令
    conda create -n gtdbtk_v2.5.2 -c conda-forge -c bioconda gtdbtk=2.5.2 python=3.12

    如果是已经有环境了，python版本是3.14，那就使用一下命令进行降级
    mamba install python=3.12 -c bioconda -c conda-forge -y





